package dev.ender.stardust.entity.cannon.medium;

import dev.ender.stardust.entity.cannon.AbstractCannonEntity;
import net.minecraft.block.BlockState;
import net.minecraft.block.entity.BlockEntityType;
import net.minecraft.util.math.BlockPos;

public class RailGun4MediumEntity extends AbstractCannonMediumEntity {
    public RailGun4MediumEntity(BlockEntityType<?> type, BlockPos pos, BlockState state) {
        super(type, pos, state);
    }
}
