package dev.ender.stardust.item.controller;

import net.minecraft.block.Block;
import net.minecraft.item.BlockItem;

public class ShipComputer4Item extends BlockItem {
    public ShipComputer4Item(Block block, Settings settings) {
        super(block, settings);
    }
}
